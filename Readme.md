# MLOps Assignment 1 - House Price Prediction

This repository implements an automated machine learning workflow using the Boston Housing dataset.

## Installation
Ensure you have Conda installed. Create and activate the environment:
```bash
conda create -n mlops_env python=3.9 -y
conda activate mlops_env
pip install -r requirements.txt
python train.py
python train2.py
